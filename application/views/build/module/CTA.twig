<section id="action-box" class="pad-25">
    <div class="container">
        <div class="action-box">
            <h3>{{ var-main-text }}</h3>
            <p>{{ var-helper-text }}</p>
            <a href="{{ var-uri }}" class="btn btn-flat flat-color">{{ var-button-text }}</a>
        </div>
        <!-- /.action-box -->
    </div>
    <!-- /.container -->
</section>
<!-- /#action-box -->